# Behaviors — Jansen & Vorster Homepage

## Header shrink
- **Trigger:** scroll past ~50–100px (theme adds `.shrink`)
- **Before:** padding `30px 0`, height `130px`, no box-shadow
- **After:** padding `15px 0`, height `100px`, `box-shadow: rgba(0,0,0,0.2) 0 1px 3px 0`, bg white
- **Transition:** `0.2s linear` on nav

## Nav links
- Uppercase, color `#000`, hover → accent `#EB5037` (theme default), transition `0.3s linear`

## Hero CTA
- Hover: slight darken (theme), cursor pointer

## Icon boxes
- Icon has `transition: 0.3s`; framed circle stays accent-colored

## Cookiebot
- Third-party overlay — **out of scope** for clone (not part of site chrome)

## Smooth scroll
- Native only; no Lenis
